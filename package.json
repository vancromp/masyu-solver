{
  "devDependencies": {
    "parcel-bundler": "^1.10.1",
    "typescript": "^3.1.1"
  },
  "scripts": {
    "dev": "parcel src/main.ts --out-file masyu-solver.user.js",
    "build": "parcel build src/main.ts --out-file masyu-solver.user.js --no-source-maps && cat src/userscript-header.js > dist/tmpfile && cat dist/masyu-solver.user.js >> dist/tmpfile && rm dist/masyu-solver.user.js && mv dist/tmpfile dist/masyu-solver.user.js"
  }
}
